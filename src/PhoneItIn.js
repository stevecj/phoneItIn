var PhoneItIn = (function(){
  function UI(){
  }
  UI.prototype = (function(){
    function bindToInput(input){
    }

    return {
      bindToInput : bindToInput
    }
  })();

  return {
    UI : UI
  };
})();
